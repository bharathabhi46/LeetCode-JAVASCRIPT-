var judgeSquareSum = function (c) {
    let i = 0, j = Math.floor(Math.sqrt(c))
    while (i <= j) {
        let sum = i * i + j * j
        if (sum === c) {
            return true
        } else if (sum < c) {
            i++
        }else {
            j--
        }
    }
return false
};
