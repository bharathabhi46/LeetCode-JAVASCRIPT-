var kthSmallest = function(matrix, k) {
let arr=[]
for(let i=0;i<matrix.length;i++){
  let n=matrix[i].length
  for(let j=0;j<n;j++){
    arr.push(matrix[i][j])
  }
}
arr.sort((a,b)=>a-b)
return arr[k-1]
};
