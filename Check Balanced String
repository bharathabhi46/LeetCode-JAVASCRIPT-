var isBalanced = function(num) {
   let n=num.length
   let e=0
   let o=0
   for(let i=0;i<n;i++){
    if(i%2!==0){
        o+=Number(num[i])
    }else{
        e+=Number(num[i])
    }
   }
   return e===o
};
