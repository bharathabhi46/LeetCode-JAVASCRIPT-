var differenceOfSum = function(nums) {
    let sum=0
    for(let i=0;i<nums.length;i++){
        sum+=nums[i]
    }
    let arr=[]
    let sum1=0
    for(let i=0;i<nums.length;i++){
        let strNum=nums[i].toString()
        for(let j=0;j<strNum.length;j++){
            sum1+=parseInt(strNum[j])
        }
    }
    return sum-sum1
};
