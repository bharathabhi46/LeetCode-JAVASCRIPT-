var xorOperation = function(n, start) {
    let arr=[]
    for(let i=0;i<n;i++){
        arr[i]=start+2*i;
    }
    let sum=arr[0]
    for(let i=1;i<arr.length;i++){
        sum^=arr[i];
    }
    return sum
};
