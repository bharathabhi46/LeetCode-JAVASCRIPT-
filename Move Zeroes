var moveZeroes = function(nums) {
 let arr=[]
 for(let i=0;i<nums.length;i++){
    arr.push(nums[i])
 }
    nums.length=0
    for(let i=0;i<arr.length;i++){
        if(arr[i]!==0){
            nums.push(arr[i])
        }
    }
    for(let i=0;i<arr.length;i++){
        if(arr[i]===0){
            nums.push(arr[i])
        }
    }
    return nums
};
